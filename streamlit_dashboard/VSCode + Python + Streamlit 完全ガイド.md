# VSCode + Python + Streamlit 完全ガイド

このガイドは、VSCode、Python、Streamlitを使用してSPI対策ダッシュボードを開発・実行するための完全なマニュアルです。

## 📋 目次

1. [環境構築](#環境構築)
2. [プロジェクト構造](#プロジェクト構造)
3. [開発ワークフロー](#開発ワークフロー)
4. [実行方法](#実行方法)
5. [カスタマイズ](#カスタマイズ)
6. [トラブルシューティング](#トラブルシューティング)
7. [GitHub連携](#github連携)

---

## 環境構築

### システム要件

| 項目 | 要件 |
|------|------|
| OS | Windows 10/11, macOS, Linux |
| Python | 3.8以上 |
| メモリ | 4GB以上推奨 |
| ディスク | 2GB以上の空き容量 |

### インストール手順

#### 1. Pythonをインストール

**Windows:**
1. [python.org](https://www.python.org/downloads/) にアクセス
2. 「Download Python 3.11」をクリック
3. インストーラを実行
4. **重要：** 「Add Python to PATH」にチェック
5. 「Install Now」をクリック

**macOS:**
```bash
brew install python@3.11
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install python3.11 python3.11-venv
```

#### 2. VSCodeをインストール

[code.visualstudio.com](https://code.visualstudio.com/) からダウンロード

#### 3. VSCode拡張機能をインストール

VSCodeを開き、以下の拡張機能をインストール：

1. **Python** (ms-python.python)
2. **Pylance** (ms-python.vscode-pylance)
3. **Streamlit** (streamlit.streamlit)
4. **Git Graph** (mhutchie.git-graph) - GitHub連携時に便利

### インストール確認

ターミナルで以下を実行：

```bash
python --version
```

出力例：
```
Python 3.11.0
```

---

## プロジェクト構造

### ディレクトリ構成

```
spi_dashboard/
├── app.py                    # メインアプリケーション
├── requirements.txt          # 依存パッケージ
├── .gitignore               # Git除外ファイル
├── README.md                # プロジェクト説明
├── VSCODE_SETUP.md          # VSCodeセットアップ
├── COMPLETE_GUIDE.md        # このファイル
├── data/
│   └── sample_log.csv       # サンプルデータ
└── venv/                    # 仮想環境（自動生成）
```

### 各ファイルの役割

| ファイル | 説明 |
|---------|------|
| `app.py` | Streamlitアプリケーション本体 |
| `requirements.txt` | Python依存パッケージリスト |
| `.gitignore` | Git除外ファイル設定 |
| `README.md` | プロジェクト概要 |
| `VSCODE_SETUP.md` | VSCodeセットアップガイド |
| `data/sample_log.csv` | サンプル学習ログ |

---

## 開発ワークフロー

### 日常の開発フロー

```
1. VSCodeでプロジェクトを開く
   ↓
2. ターミナルで仮想環境を有効化
   ↓
3. Streamlitアプリを起動
   ↓
4. コードを編集
   ↓
5. ファイルを保存（自動再読み込み）
   ↓
6. ブラウザで動作確認
   ↓
7. 必要に応じて手順4に戻る
```

### コード編集のベストプラクティス

#### 1. 変更は小さく、頻繁に

```python
# ❌ 悪い例：大きな変更を一度にする
def large_change():
    # 100行の変更

# ✅ 良い例：小さな変更を段階的に
def small_change_1():
    # 10行の変更

def small_change_2():
    # 10行の変更
```

#### 2. コメントを活用

```python
# ✅ 良い例：何をしているか明確
# ユーザーの入力から企業名を取得
company = st.text_input("志望企業名")

# ❌ 悪い例：コメントがない
company = st.text_input("志望企業名")
```

#### 3. 関数に分割

```python
# ✅ 良い例：関数に分割して再利用性を高める
def calculate_metrics(df):
    """メトリクスを計算"""
    return {...}

def display_kpi_cards(metrics):
    """KPIカードを表示"""
    st.markdown(...)

# ❌ 悪い例：すべてメイン処理に書く
# 計算処理とUI処理が混在
```

---

## 実行方法

### 初回セットアップ

#### ステップ1: プロジェクトフォルダをVSCodeで開く

```bash
# ターミナルで実行
code /path/to/spi_dashboard
```

または、VSCodeのメニューから「File」→「Open Folder」

#### ステップ2: 仮想環境を作成

VSCodeのターミナル（Ctrl+``）で：

```bash
python -m venv venv
```

#### ステップ3: 仮想環境を有効化

**Windows (PowerShell):**
```powershell
.\venv\Scripts\Activate.ps1
```

**Windows (Command Prompt):**
```cmd
venv\Scripts\activate.bat
```

**macOS/Linux:**
```bash
source venv/bin/activate
```

#### ステップ4: 依存パッケージをインストール

```bash
pip install -r requirements.txt
```

#### ステップ5: アプリを起動

```bash
streamlit run app.py
```

### 毎回の起動方法

```bash
# 1. VSCodeでプロジェクトを開く
code /path/to/spi_dashboard

# 2. ターミナルで仮想環境を有効化
# Windows: .\venv\Scripts\activate.bat
# macOS/Linux: source venv/bin/activate

# 3. アプリを起動
streamlit run app.py
```

### ブラウザでアクセス

自動的にブラウザが開き、以下のURLでアクセス可能：

- **ローカル:** `http://localhost:8501`
- **ネットワーク:** `http://169.254.0.21:8501`

### アプリを停止

ターミナルで `Ctrl+C` を押す

---

## カスタマイズ

### 1. カラーを変更

`app.py` の先頭部分を編集：

```python
# ===== カラー定義 =====
PRIMARY = "#3B82F6"      # プライマリ色（青）
ACCENT = "#F97316"       # アクセント色（オレンジ）
SUCCESS = "#10B981"      # 成功色（緑）
WARNING = "#F59E0B"      # 警告色（黄）
DANGER = "#EF4444"       # 危険色（赤）
NEUTRAL = "#6B7280"      # ニュートラル色（グレー）
```

**カラーコード参考：**
- 青：`#3B82F6`, `#1E40AF`, `#0369A1`
- 赤：`#EF4444`, `#DC2626`, `#991B1B`
- 緑：`#10B981`, `#059669`, `#047857`
- オレンジ：`#F97316`, `#EA580C`, `#C2410C`

### 2. 問題マスタを追加

`app.py` の `DEFAULT_MASTER_ROWS` に追加：

```python
DEFAULT_MASTER_ROWS = [
    # 既存の問題...
    ["N-A01", "非言語", "推論", "集合の推論 (ベン図)", 120, 85, "高", 5],
    
    # 新しい問題を追加
    ["N-A07", "非言語", "推論", "新しい単元", 120, 85, "高", 5],
    ["N-A08", "非言語", "推論", "別の新しい単元", 100, 80, "中", 4],
]
```

**フォーマット：**
```
[問題ID, 科目, ジャンル, 単元, 目標解答時間(秒), 目標正答率(%), 難易度, 出題頻度(重み)]
```

### 3. ページタイトルを変更

```python
st.set_page_config(
    page_title="新しいタイトル",  # ここを変更
    page_icon="📚",
    layout="wide",
    initial_sidebar_state="expanded"
)
```

### 4. デフォルト値を変更

```python
# デフォルト目標正答率を70%に変更
if "target_rate_user" not in st.session_state:
    st.session_state.target_rate_user = 70  # ここを変更
```

### 5. 新しい機能を追加

例：学習時間の合計を表示

```python
# データ処理セクションに追加
total_study_time = df["学習投入時間(分)"].sum()

# メインコンテンツに追加
st.metric("総学習時間", f"{total_study_time}分")
```

---

## トラブルシューティング

### エラーメッセージ別対応

#### 1. `streamlit: command not found`

**原因:** 仮想環境が有効になっていない

**解決方法:**
```bash
# Windows
.\venv\Scripts\activate.bat

# macOS/Linux
source venv/bin/activate
```

#### 2. `ModuleNotFoundError: No module named 'streamlit'`

**原因:** パッケージがインストールされていない

**解決方法:**
```bash
pip install -r requirements.txt
```

#### 3. ポート8501が既に使用されている

**原因:** 別のStreamlitプロセスが実行中

**解決方法:**
```bash
# 別のポートで実行
streamlit run app.py --server.port 8502
```

#### 4. テキストが見えない

**原因:** ブラウザのキャッシュ問題

**解決方法:**
- ブラウザをリロード（F5キー）
- または Ctrl+Shift+Delete でキャッシュをクリア

#### 5. グラフが表示されない

**原因:** Plotlyがインストールされていない

**解決方法:**
```bash
pip install --upgrade plotly
```

#### 6. 日本語が文字化けする

**原因:** ファイルのエンコーディング設定

**解決方法:**
- VSCodeで `Ctrl+Shift+P`
- 「Change File Encoding」を検索
- 「UTF-8」を選択

### デバッグ方法

#### 1. print()で出力

```python
# app.py に追加
print("デバッグ情報:", variable)

# ターミナルに出力される
```

#### 2. st.write()で表示

```python
# ダッシュボードに表示
st.write("デバッグ情報:", variable)
```

#### 3. ブレークポイント

1. `app.py` の行番号左をクリック（赤い点）
2. F5キーを押してデバッグ開始
3. 実行が一時停止

---

## GitHub連携

### 1. GitHubアカウントを作成

[github.com](https://github.com/) でアカウント作成

### 2. リポジトリを作成

1. GitHubにログイン
2. 「New」をクリック
3. Repository nameに「spi_dashboard」と入力
4. 「Create repository」をクリック

### 3. ローカルリポジトリを初期化

```bash
cd /path/to/spi_dashboard
git init
git add .
git commit -m "Initial commit"
```

### 4. リモートリポジトリを設定

```bash
git remote add origin https://github.com/yourusername/spi_dashboard.git
git branch -M main
git push -u origin main
```

### 5. VSCodeで確認

VSCodeの左サイドバーの「Source Control」でGitの状態を確認

---

## 本番環境へのデプロイ

### Streamlit Community Cloudへのデプロイ

1. GitHubにコードをプッシュ
2. [Streamlit Community Cloud](https://streamlit.io/cloud) にアクセス
3. GitHubアカウントで認証
4. 「New app」をクリック
5. リポジトリ、ブランチ、ファイルを選択
6. 「Deploy」をクリック

### デプロイ後

- 公開URL が発行される
- 誰でもアクセス可能
- コードを更新すると自動的に再デプロイ

---

## パフォーマンス最適化

### 1. キャッシュを活用

```python
@st.cache_data
def load_data():
    """データ読み込みをキャッシュ"""
    return pd.read_csv("data.csv")

df = load_data()
```

### 2. 不要な再計算を避ける

```python
# ❌ 悪い例：毎回計算
result = expensive_calculation()

# ✅ 良い例：セッションに保存
if "result" not in st.session_state:
    st.session_state.result = expensive_calculation()
result = st.session_state.result
```

### 3. 大きなデータセットは絞る

```python
# ❌ 悪い例：全データを処理
df_all = pd.read_csv("large_file.csv")

# ✅ 良い例：必要な部分だけ処理
df_filtered = pd.read_csv("large_file.csv", usecols=["date", "value"])
```

---

## セキュリティ

### 1. 秘密情報を保護

```python
# ❌ 悪い例：コードに直接記述
API_KEY = "sk-1234567890"

# ✅ 良い例：環境変数から読み込み
import os
API_KEY = os.getenv("API_KEY")
```

### 2. ユーザー入力を検証

```python
# ✅ 良い例：入力を検証
company = st.text_input("企業名")
if len(company) > 100:
    st.error("企業名は100文字以内です")
```

---

## よくある質問

### Q1: 複数のユーザーで同時に使用できる？

A: Streamlit Community Cloudなら可能です。ローカル実行は1ユーザーのみ。

### Q2: データベースを使用できる？

A: はい。SQLiteやPostgresQLなどと連携可能です。

### Q3: 定期実行できる？

A: Streamlit Cloudの場合、GitHub Actionsと組み合わせて可能です。

### Q4: ダウンロード機能は？

A: `st.download_button()` で実装可能です。

### Q5: ログイン機能は？

A: Streamlit Authなどのライブラリで実装可能です。

---

## 参考資料

### 公式ドキュメント

- [Streamlit Documentation](https://docs.streamlit.io/)
- [Python Documentation](https://docs.python.org/)
- [Pandas Documentation](https://pandas.pydata.org/docs/)
- [Plotly Documentation](https://plotly.com/python/)

### チュートリアル

- [Streamlit Tutorial](https://docs.streamlit.io/library/get-started)
- [Python for Data Analysis](https://wesmckinney.com/book/)

### コミュニティ

- [Streamlit Community](https://discuss.streamlit.io/)
- [Stack Overflow](https://stackoverflow.com/questions/tagged/streamlit)

---

## まとめ

このガイドで以下を学びました：

- ✅ VSCode + Python + Streamlitの環境構築
- ✅ プロジェクト構造の理解
- ✅ 開発ワークフロー
- ✅ カスタマイズ方法
- ✅ トラブルシューティング
- ✅ GitHub連携
- ✅ デプロイ方法

次のステップは、GitHub連携してStreamlit Cloudでデプロイすることです！

質問や問題がある場合は、GitHubのissueを作成してください。
